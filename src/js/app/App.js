import React from 'react';
/*
import <Appname> from './<Appname>' // for component with container only
import <Appname> from '../<appname>/<Appname>'
*/

import Sample from '../sample/Sample';

export default () => (
  <div>
    <Sample />
  </div>
);
